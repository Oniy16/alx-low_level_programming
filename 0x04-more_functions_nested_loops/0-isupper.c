#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	char c;
	if ( _isupper(c) == 1){
		printf("%c: %d\n", c, _isupper(c));
		return ( _isupper(c));
	}
	else
		return (0)
}
