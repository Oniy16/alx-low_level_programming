#include "main.h"

/**
 * _isupper - check the code.
 * @x: The number to be check
 * Return: 0 for none uper case 1 for upper case
 */
int _isupper(int x)
{

	if (x >= 65 && x <= 90)
	{
		return(1);
	}

		return (0);
}
