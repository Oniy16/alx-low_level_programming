#include "main.h"

/**
 * _mul - Check if a character is a digit
 * @a: The 1st number to be mul
 * @b: The 2nd number to be mul
 * Return: 0 for any correct one
 */
int mul(int a, int b)
{
	int x;

	x = a * b;
	return (x);
}
