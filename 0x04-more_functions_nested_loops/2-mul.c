#include "main.h"

/**
  * _isdigit - Check if a character is a digit
  * @x: The number to be checked
  *
  * Return: 0 for any correct one
  */
int mul(int a, int b)
{
	int x;

	x = a * b;
	return (x);
}
